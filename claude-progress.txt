# Claude Progress — cers-index

## 2026-07-09 — 홈 상단 소개 숨김 및 KPI 표 헤더 단순화

### 완료 작업
- 홈 최상단 소개·방법론 위젯을 JSX 주석 처리해 화면에서 숨김
- 홈과 `/companies/score-list`의 CERs Index List 카테고리 헤더를
  미확정 영문 명칭 대신 `KPI1`~`KPI4`로 표시
- 카테고리 데이터와 방법론/About 화면의 정식 명칭은 유지

### 검증
- Node.js 20.19.0, `npm run check` 통과
- dev 홈 HTML에서 숨긴 소개 문구가 렌더링되지 않음을 확인
- 실제 DB 점수가 0건이라 KPI 표 헤더 런타임 분기는 렌더링되지 않았으며,
  두 표 구현과 production build로 변경을 검증

## 2026-07-08 — 홈 하이드레이션 경고 수정

### 배경
브라우저에서 `/` 홈 진입 시 `AppShell` 루트 주변에
`Hydration failed because the server rendered HTML didn't match the client` 경고가 발생.
첨부 스택은 서버 쪽 `<Suspense>`와 클라이언트 쪽 `AppShell` `<div>`가 맞지 않는
형태를 가리킴.

### 완료 작업
- `AppShell`에서 `SiteHeader` 전체를 감싸던 Suspense fallback 제거
- `SiteHeader`의 `useSearchParams()` 의존 제거
  - 검색어와 query string은 mount 후 `window.location.search`에서 동기화
  - 검색 input은 controlled state로 전환
- `/`와 `/ko` dev HTML에서 `AppShell` `<div>` 다음에 실제 `<header>`가 바로
  렌더되고, 헤더 앞 Suspense fallback 마커가 사라진 것 확인

### 검증
- Node.js 20.19.0, `npm run check` 통과
  - harness/lint/typecheck/Vitest 4 files, 14 tests 통과
  - production build 통과
- `npm run dev` on `127.0.0.1:3001`
  - `curl -s http://127.0.0.1:3001/`
  - `curl -s http://127.0.0.1:3001/ko`
  - 두 응답 모두 `AppShell` root `<div>` 다음 실제 `header` 확인

### 잔여/주의
- 현재 환경에는 Playwright/브라우저 런타임 패키지가 없어 실제 브라우저 콘솔까지
  자동 검증하지는 못함.
- F01은 여전히 active 유지. 홈 전체 passing 전환은 실제 브라우저 사용자 흐름 확인 후 진행.

## 2026-06-29 — ver2 문서 기준 전반 정합 (잔재 제거)

### 배경
`docs/LOGIC/CERs Index for Company_ver2.md`를 단일 기준으로 코드 전반을 재대조.
방법론 화면(score-logic-v3.tsx)은 이미 12개 변수 산식과 정확히 일치함을 확인.
나머지 파일에 남아 있던 v1.4 잔재를 제거.

### 완료 작업
- `score-logic-v3.tsx` 대조: V1(0.042·S_absolute), V2(Scope3 절대량), W1(체크리스트
  곱·목표 평균), W2(P_t), C3(100/25/0), C4(m/4), A1((a1+a2)/2), A2(¼식),
  A3·A4(m·p/4), 최종 K_j·CERs 모두 문서와 일치 — 수정 불필요
- KPI2 라벨 영어 정합: 삭제된 v1.4 "ambition(야심도)" 잔재 제거
  - `i18n.ts` en 4곳: "Target ambition" → "Target design & delivery"
    (ko/ja는 이미 "목표 및 이행"/"削減目標と履行"로 정리돼 있었음)
  - 카테고리 라벨 `public.ts`·`fallback-data.ts`: "Target Ambition & Delivery"
    → "Target Design & Delivery"
- 버전 라벨 정합: `fallback-data.ts` methodologyVersion "CERs v0.1" → "CERs v1.5" (2곳)
- dead code 제거: 미import 확인된 `components/cers/score-logic.tsx`(v1.4 16변수) 삭제
- `feature_list.json` F07/F08: 설명·evidence를 v1.4/16변수 → ver2(v1.5)/12변수로 갱신
- 테스트 갱신: `public.test.ts`·`kpi-score-grid.test.tsx`의 옛 "ambition" 라벨 단언 수정

### 검증
- Node.js 20.19.0, `npm run check:quick` 통과 (harness/lint/typecheck/test)
- Vitest 4 files, 14 tests 통과
- src 전수 스캔: v1.4/v0.1/16변수/ambition 라벨 잔재 0건

### 잔여/주의
- public.ts의 "ambition statements"(607·757), score-logic-v3 v1 설명의 "ambition and
  delivery"(286)는 일반 영어 표현으로 삭제된 변수와 무관 — 유지
- 소비 화면(홈/기업/비교/섹터)의 12개 변수 개별 점수 표시는 여전히 batch가 변수별
  점수 계약을 제공할 때 Phase 2로 구현 (현 상태 4 KPI 카테고리 수준 렌더 유지)

## 2026-06-24 — CERs Index ver2(v1.5) 12개 변수 화면 반영

### 배경
`docs/LOGIC/CERs Index for Company_ver2.md`로 로직 업데이트. 16개 변수 →
12개 변수로 구조 변경(n1=2, n2=2, n3=4, n4=4). 대분류 4 KPI는 유지.

### 변수 구조 변화 (v1.4 → ver2/v1.5)
- KPI1: V1(S1·2), V2(S3 절대감축, ÷0.025·S_absolute 제거). V3 재생에너지 삭제.
- KPI2: W1 감축목표 설계 수준(7항목 체크리스트 곱·목표 평균), W2 이행 진척도(구 W3).
  구 W1 야심도·W2 커버리지 삭제, W4 SBTi 검증은 A1로 이동.
- KPI3: C1·C2 유지, C3 내부탄소가격 연속→3단계(100/25/0), C4 유지.
- KPI4: A1 제3자 검증(SBTi a1 + 보증 a2 결합), A2·A3 유지, A4 공시정합(구 A5).
  구 A4 시계열 일관성 삭제.

### 완료 작업
- `score-logic-v3.tsx` 전면 재작성: 12개 변수, ko/en/ja, 산식·근거·상호견제 ver2 반영
- `i18n.ts` ko/en/ja: kpi.items 변수개수·methodLink·proofFramework·scoreMeaning·
  about(정의 n_j=2/2/4/4·meaningPoints·logicSteps·dimensions) 12개 변수로 전환
- 방법론 버전 라벨 v1.4 → v1.5 (홈/헤더/리더보드/기업상세/score-logic eyebrow)
- `fallback-data.ts` 카테고리 라벨을 4 KPI 정식명으로, 가중치 동일가중 0.25
- 테스트: score-logic-v3.test.tsx(12개 변수 타이틀), public.test.ts(v1.5) 갱신
- `docs/screen/README.md` 12개 변수 구조·anchor·전환신호·비교표로 갱신

### 검증
- Node.js 20.19.0, `npm run check:quick` 통과 (harness/lint/typecheck/test)
- Vitest 4 files, 14 tests 통과

### 잔여/주의
- `src/components/cers/score-logic.tsx`(1093줄, v1.4 16변수)는 어떤 라우트에도
  import되지 않는 dead code. README §9에서 제거 후보로 명시됨 — 삭제는 미시행.
- 소비 화면(홈/기업/비교/섹터)은 4 KPI 카테고리 수준으로만 렌더하므로 12개 변수
  개별 점수 화면은 batch가 변수별 점수 계약을 제공할 때 Phase 2로 구현.

## 2026-06-11 — CERs Index v1.4 방법론 화면 전환

### 완료 작업

- `docs/LOGIC/`의 v1.4 방법론을 기준으로 About 개요를 4 KPI·16개 변수
  구조와 동일가중 집계 방식으로 교체
- `/about/logic`과 `/score-logic`이 사용하는 상세 로직 화면을 한국어,
  영어, 일본어로 전환
- 3개 언어에서 16개 변수가 모두 렌더링되고 구 V1~V9/P2/산업별 가중치
  설명이 남지 않는지 검사하는 Vitest 추가
- 향후 전체 화면 개편 기준을 `docs/screen/README.md`에 작성
  - 홈의 공개 탄소감축 대시보드 표와 점수소개 영역은 유지
  - 나머지 화면은 v1.4의 4 KPI·16개 변수 구조로 재설계
- 대시보드 서버 조회에 남아 있던 강제 목데이터 반환을 제거해 실제 DB 조회
  경로를 복구

### 검증

- Node.js 20.19.0
- `npm run check:quick`: 통과
- Vitest: 2 files, 7 tests 통과
- `npm run check`: 통과
- production build에서 `/about`, `/about/logic`, `/score-logic`과 다국어
  라우트 생성 확인

## 2026-06-10 — 하네스 엔지니어링 세팅 + 스키마 통일

### 배경
CERsIndex-batch 파이프라인이 적재한 DB(Supabase)와 cers-index 프론트가 기대하는
테이블명이 완전히 달랐음. 화면 구조는 유지한 채 스키마를 통일하도록 요청.

### 완료 작업

#### 1. DB Views 설계 및 파일화 (`docs/views.sql`)
batch 스키마 → 프론트 기대 이름 매핑 views 11개:

| View | 소스 | 상태 |
|------|------|------|
| `companies` | `company` | SQL 작성 완료 |
| `documents` | `report` | SQL 작성 완료 |
| `rpt_period` | `clean_val` | SQL 작성 완료 |
| `co_metric` | `clean_val` (배출·에너지) | SQL 작성 완료 |
| `co_target` | `clean_val` pivot | SQL 작성 완료 |
| `doc_fw_adopt` | `report.frame_json` | SQL 작성 완료 |
| `score_categories` | `kpi` | SQL 작성 완료 |
| `method_ver` | `score_run` | SQL 작성 완료 |
| `scoring_runs` | `final_score` | SQL 작성 완료 |
| `cers_score` | `final_score` | SQL 작성 완료 |
| `category_scores` | `kpi_score` | SQL 작성 완료 |

⚠️ **DB 미적용**: Supabase MCP read-only 제약으로 SQL 실행 불가.
수동 적용 필요 → `docs/views.sql` 참고.

#### 2. 하네스 파일 구축
- `CLAUDE.md` 신규: 아키텍처, DB 연결, views 변경 절차, 제약사항
- `AGENTS.md` 신규: 운영 매뉴얼 (기술 스택, 피처 추적, 세션 규칙)
- `feature_list.json` 신규: 화면별 피처 상태 추적
- `session-handoff.md` 신규: 세션 상태 핸드오프
- `clean-state-checklist.md` 신규: 세션 종료 전 체크리스트
- `.env.example` 신규: 필요 환경변수 템플릿

#### 3. vitest 수정
- 문제: `setup.ts`에 테스트 코드 혼입 → glob 불일치로 `No test files found`
- 수정: `setup.ts` → 빈 setup 파일 / 테스트는 `smoke.test.ts` 분리
- 결과: `npm run test` 1 pass ✅

#### 4. 구버전 파일 정리
- 구 Vite/dashboard 컴포넌트 30여 개 삭제 커밋 (cers_index_ver2 브랜치)

### 현재 상태 (2026-06-10 업데이트)
- 브랜치: `cers_index_ver2`
- 커밋: `bf0fa68`
- DB views: **적용 완료** (Node.js pg 스크립트로 실행)
  - 11개 views 모두 생성 확인
- 화면 동작:
  - 넷제로 선언 기업 섹션 → 실제 DB 데이터 표시 ✅
  - CERs Index List / 섹터별 → "데이터 부족" (F05 스코어 미실행 탓) ⚠️
  - 개발 서버: Node 20 + `npm run dev` 정상 기동 (Node 18 사용 불가)

### 다음 작업
1. CERsIndex-batch **F05 스코어링 엔진** 실행 → `final_score`, `kpi_score` 채워짐
2. 스코어 적재 후 Index List / 섹터별 화면 데이터 확인
3. (선택) 기업 상세 페이지 데이터 확인

## 2026-06-10 — cers-index 하네스 실행 계층 보강

### 완료

- `AGENTS.md`를 짧은 진입 지도와 WIP=1/완료 조건 중심으로 정리
- `CLAUDE.md`를 `AGENTS.md` 참조 파일로 축소해 규칙 중복 제거
- 환경, 아키텍처, 테스트, 설계 결정, 품질 문서를 `docs/`로 분리
- `scripts/check-harness.mjs` 추가
  - Node 20.19 이상
  - 필수 하네스 파일
  - `feature_list.json` 상태 계약
  - Client Component의 server 모듈 import 금지
  - 비밀정보·생성물 ignore 규칙 검사
- `npm run check:harness`, `check:quick`, `check` 검증 명령 추가
- GitHub Actions에서 Node 20.19 + `npm run check` 실행하도록 구성
- ESLint가 `.next` 생성물을 검사하던 문제 수정
- `feature_list.json`을 WIP=1 기준으로 정리

### 검증

- `npm run check:quick`: 통과
  - harness, lint, typecheck, Vitest 1/1
- `npm run build`: Node 20.19에서 통과
- `npm run dev`: Ready 상태까지 확인
- HTTP 화면 확인은 WSL 안정성 요청에 따라 중단

### 운영 주의

- 기본 셸 Node가 18일 수 있으므로 모든 npm 작업 전에 `nvm use` 필요
- WSL에서 이미 실행 중인 웹 프로세스와 충돌하거나 WSL이 종료될 수 있어,
  웹/E2E 검증은 사용자 요청이 있을 때만 제한적으로 수행
- `.env.example` 삭제와 `docs/harness-engineering/` 미추적 상태는 이번 작업 이전부터
  존재한 변경으로 유지
